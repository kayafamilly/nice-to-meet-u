import Link from "next/link";
import { LocalDate, ManagementHeading, Pager, Status } from "@/components/management-ui";
import { managementData } from "@/lib/management/data";
import type { ManagementModerationReport, ManagementPage } from "@/types/api";
export default async function ModerationPage({searchParams}:{searchParams:Promise<{page?:string}>}){const page=Math.max(1,Number((await searchParams).page)||1);const data=await managementData<ManagementPage<ManagementModerationReport>>("moderation",{page});return <><ManagementHeading eyebrow="Safety visibility" title="Moderation" copy="Read-only review of concerns submitted by session participants."/><section className="management-report-list">{data.items.length?data.items.map((report)=><article className="management-report" key={report.id}><div><Status value={report.status}/><span className="management-tag">{report.reason.replaceAll("_"," ")}</span></div><h2>{report.reportedUserName?`Concern involving ${report.reportedUserName}`:"Session concern"}</h2><p>{report.details||"No additional details were provided."}</p><footer><span>Reported by <Link href={`/management/users/${report.reporterId}`}>{report.reporterName}</Link></span>{report.sessionId&&<Link href={`/management/sessions/${report.sessionId}`}>View session →</Link>}<LocalDate value={report.createdAt}/></footer></article>):<div className="management-empty"><strong>No reports</strong><p>The moderation queue is clear.</p></div>}</section><Pager base="/management/moderation" page={data.page} totalPages={data.totalPages}/></>}
