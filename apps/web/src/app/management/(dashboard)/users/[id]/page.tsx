import Link from "next/link";
import { LocalDate, ManagementHeading, Metric, Status } from "@/components/management-ui";
import { managementData } from "@/lib/management/data";
import type { ManagementUserDetail } from "@/types/api";
export default async function UserDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await managementData<ManagementUserDetail>("user",{id});return <><Link className="management-back" href="/management/users">← Users</Link><ManagementHeading eyebrow="User profile" title={user.displayName} copy={user.email} actions={<Status value={user.verified?user.status:"unverified"}/>}/><section className="management-metrics"><Metric label="Sessions" value={user.sessionStats.total}/><Metric label="Attended" value={user.sessionStats.attended} tone="aqua"/><Metric label="No-shows" value={user.sessionStats.noShow} tone="coral"/><Metric label="Cancelled" value={user.sessionStats.cancelled} tone="neutral"/></section><section className="management-split"><article className="management-panel"><p className="eyebrow">Account</p><dl className="management-definition"><div><dt>Created</dt><dd><LocalDate value={user.createdAt}/></dd></div><div><dt>Verified</dt><dd>{user.verified?"Yes":"No"}</dd></div><div><dt>Onboarding</dt><dd>{user.onboardingComplete?"Complete":"Incomplete"}</dd></div><div><dt>Timezone</dt><dd>{user.timeZone}</dd></div><div><dt>Suspension</dt><dd><LocalDate value={user.suspendedUntil}/></dd></div></dl></article><article className="management-panel"><p className="eyebrow">Languages</p><div className="management-language-list">{user.languages.map((language)=><div key={`${language.name}-${language.level}`}><strong>{language.name}</strong><span>{language.native?"Native":language.level}</span></div>)}</div></article></section><section className="management-panel"><h2>Session history</h2><div className="management-table-wrap"><table className="management-table"><thead><tr><th>Language</th><th>Date</th><th>Status</th><th>People</th><th></th></tr></thead><tbody>{user.sessions?.map((session)=><tr key={session.id}><td>{session.languageName}</td><td><LocalDate value={session.startsAt}/></td><td><Status value={session.status}/></td><td>{session.participantCount}/4</td><td><Link href={`/management/sessions/${session.id}`}>Open →</Link></td></tr>)}</tbody></table></div></section></>}
