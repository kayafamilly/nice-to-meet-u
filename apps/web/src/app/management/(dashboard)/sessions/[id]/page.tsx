import Link from "next/link";
import { LocalDate, ManagementHeading, Metric, Status } from "@/components/management-ui";
import { managementData } from "@/lib/management/data";
import type { ManagementSessionDetail } from "@/types/api";
export default async function SessionDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const session=await managementData<ManagementSessionDetail>("session",{id});return <><Link className="management-back" href="/management/sessions">← Sessions</Link><ManagementHeading eyebrow="Session detail" title={`${session.languageName} conversation`} copy={session.note||"No optional note"} actions={<Status value={session.status}/>}/><section className="management-metrics"><Metric label="Reserved people" value={`${session.participantCount}/4`}/><Metric label="Host" value={session.hostName} tone="aqua"/><Metric label="Starts" value={<LocalDate value={session.startsAt}/>} tone="coral"/><Metric label="Duration" value="30 min" tone="neutral"/></section><section className="management-panel"><div className="management-panel-head"><div><p className="eyebrow">Participants</p><h2>Attendance and roles</h2></div></div><div className="management-table-wrap"><table className="management-table"><thead><tr><th>Person</th><th>Role</th><th>Reservation</th><th>Joined</th><th>Left</th></tr></thead><tbody>{session.participants.map((person)=><tr key={person.id}><td><Link href={`/management/users/${person.userId}`}>{person.displayName}</Link></td><td><Status value={person.role}/></td><td><Status value={person.reservationStatus}/></td><td><LocalDate value={person.joinedAt}/></td><td><LocalDate value={person.leftAt}/></td></tr>)}</tbody></table></div></section></>}
